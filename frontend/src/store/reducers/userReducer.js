
const inital = {
    logged: false,
    data: {}
}

const userReducer = (state = inital, action) => {

    switch (action.type) {

        default: return state
    }
}

export default userReducer










